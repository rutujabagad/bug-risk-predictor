spring:
  datasource:
    url: jdbc:mysql://localhost:3306/code_analyzer
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jenkins:
  url: http://localhost:8080
  username: admin
  token: my-jenkins-token

sonarqube:
  url: http://localhost:9000
  token: my-sonar-token
